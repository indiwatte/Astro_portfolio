---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout title="Indi Watte | Home">
	<section class="hero">
		<h1 class="title">portfolio</h1>
		<img
			src="/src/assets/images/profiel.png"
			alt="Photo of Indi"
			class="intro-image"
		/>
	</section>

	<section class="introduction">
		<p class="tagline">Hi, I’m Indi Watte — a DEVINE student at Howest.</p>
		<p class="intro-text">
			Welcome to my portfolio — explore my projects, experiments, and
			ideas.
		</p>
	</section>

	<section class="devine-explanation">
		<h2>Welcome to my playground!</h2>
		<p>
			Digital Design & Development is all about combining creativity with
			technology — crafting beautiful, functional digital experiences. It
			brings together design, coding, and storytelling to build websites,
			apps, and interactive projects that don’t just look good but also
			feel right to use. It’s where visual design meets smart development
			to create meaningful digital products.
		</p>

		<a href="/projects" class="button">View all projects</a>
	</section>

	<section class="featured-projects">
		<h2>Featured Projects</h2>
		<p>Here you can already see some of my projects featured.</p>

		<!-- Horizontal scroll area -->
		<div class="featured-scroll" role="list">
			<article class="card card1" role="listitem">
				<div class="text-bg">
					<h3 class="card-title">rocket click game</h3>
					<p class="card-description">
						My first game making experience
					</p>
					<a class="card-button" href="/projects/click-game"
						>View Project</a
					>
				</div>
			</article>

			<article class="card card2" role="listitem">
				<div class="text-bg">
					<h3 class="card-title">Thrive Campaign</h3>
					<p class="card-description">
						I designed a campaign website for Thrive Belgium
					</p>
					<a class="card-button" href="/projects/thrive-campaign"
						>View Project</a
					>
				</div>
			</article>

			<article class="card card3" role="listitem">
				<div class="text-bg">
					<h3 class="card-title">Project Three</h3>
					<p class="card-description">
						Short description of project three — a quick preview.
					</p>
					<a class="card-button" href="/projects/third-project"
						>View Project</a
					>
				</div>
			</article>
		</div>
	</section>

	<style>
		:root {
			--orange: orange;
			--pink: #ff69b4;
			--black: #000;
			--white: #fff;
		}

		body {
			background-color: var(--black);
			color: var(--white);
			font-family:
				system-ui,
				-apple-system,
				"Segoe UI",
				Roboto,
				sans-serif;
			margin: 0;
			padding: 0;
		}

		/* 	.text-bg {
			background-color: white;
			max-width: fit-content;
			padding: 1rem;
		} */

		.hero {
			display: flex;
			flex-direction: column;
			align-items: center;
			justify-content: center;
			padding: 4rem 1rem;
			text-align: center;
		}

		.title {
			font-size: 15rem;
			text-transform: uppercase;
			text-decoration: underline;
			text-decoration-color: var(--purple);
			margin-bottom: -5rem;
			z-index: 1;
		}

		.intro-image {
			width: 600px;
			height: 600px;
			object-fit: cover;
			position: absolute;
			z-index: 2;

			left: 70%; /* center horizontally */
			top: 35%; /* move to top */
			transform: translateX(-50%); /* compensate for width */
		}
		h2 {
			font-weight: 800;
			font-size: 3rem;
			text-transform: uppercase;
			margin: 0 0 1rem 0;
			color: var(--green);
		}

		h3 {
			font-weight: 800;
			font-size: 5rem;
			color: var(--purple);
		}

		p {
			font-size: 1.2rem;
			line-height: 1.6rem;
			max-width: 800px;
		}

		.devine-explanation {
			background-color: var(--fluo-yellow);
		}

		.introduction,
		.devine-explanation,
		.featured-projects {
			display: flex;
			flex-direction: column;
			align-items: flex-start;
			text-align: left;
			padding: 3rem 1rem;
			gap: 1rem;
			border-top: 1px solid #333;
		}

		.introduction {
			border-top: none;
			padding-top: 2rem;
			padding: 8rem;
		}
		.button,
		.card-button {
			display: inline-block;
			padding: 0.75rem 1.5rem;
			background: linear-gradient(135deg, var(--purple), var(--bg-color));
			color: #000;
			text-decoration: none;
			font-weight: 600;
			border-radius: 6px;
			margin: 1.5rem;
			transition:
				transform 0.2s ease,
				box-shadow 0.2s ease;
			border: none;
			cursor: pointer;
		}

		.button:hover,
		.card-button:hover {
			transform: translateY(-3px);
			box-shadow: 0 6px 14px rgba(0, 0, 0, 0.2);
		}

		.button:active,
		.card-button:active {
			transform: translateY(0);
			box-shadow: 0 3px 8px rgba(0, 0, 0, 0.15);
		}
	</style>
</BaseLayout>

<!-- /* ---------- HORIZONTAL SCROLL STYLES ---------- */

		.featured-scroll {
			display: flex;
			flex-direction: row;
			gap: 1rem;
			overflow-x: auto;
			padding: 1rem 0;
			scroll-snap-type: x mandatory; /* optional snap */
			-webkit-overflow-scrolling: touch;
		}

		

		/* Each card takes up the viewport width but leaves a small gap.
		   Use min-width: 90vw for a little peek of the next card, or 100vw for full-bleed */
		.card,
		.card1 {
			flex: 0 0 90vw; /* do not shrink, fixed to 90% of viewport width */
			height: 400px;
			background-image: url("/assets/images/placeholder.png");
			background-size: 40%;
			background-repeat: no-repeat;
			background-position: center;
			border: 1px solid #222;
			padding: 1.25rem;
			box-sizing: border-box;
			border-radius: 8px;
			display: flex;
			flex-direction: column;
			justify-content: center;
		}

		.card + .card {
			/* small visual separation handled by gap already */
		}

		.card-title {
			font-size: 1.4rem;
			margin: 0 0 0.5rem 0;
		}

		.card-description {
			margin: 0;
			color: var(--pink);
			font-size: 1rem;
		} -->
